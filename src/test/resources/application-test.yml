  server:
    port: 8282

  spring:

    application:
      name: ITestTaskManager

    datasource:
      url: jdbc:tc:postgresql:17:///test_db
      driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

    jpa:
      hibernate:
        ddl-auto: create
      show-sql: true
